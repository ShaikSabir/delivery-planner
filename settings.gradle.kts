rootProject.name = "DeliveryPlanner"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }

    plugins {
        kotlin("jvm") version "1.9.0"
        kotlin("kapt") version "1.9.0"
    }
}